# Story 6.11: Collapse NL Answer Paths — Single Orchestrator, FAQ Cache, Tier-2 Escalation

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As Nicolas (operator of the Maps of Making Bernard bot),
I want exactly one system answering natural-language questions — `agent.py`'s tool-calling loop, with `nl_to_sparql.py`'s SPARQL-generation folded in as a callable tool and a small FAQ cache in front of it — instead of two disagreeing NL systems (`router.py`'s `nl_discovery` branch calling `nl_to_sparql.dispatch()` directly, vs. `unknown`/`query`/`write` calling `agent.run()`),
so that a question like "how many spaces are open now in Berlin and which one?" gets one consistent answer, on the project's configured default model, with one gap-log mechanism — not a coin-flip between two code paths with different models and different failure behavior.

This is the collapse-first slice of the target architecture from `_bmad-output/planning-artifacts/mom_handoff_2026-07-02.md` and tonight's roundtable resolution of the Winston/Nicolas fork: **`agent.py` is the orchestrator, not `nl_to_sparql.py`.** Tier 0 (FAQ semantic cache) and Tier 2 (Sonnet escalation) both live inside/in-front-of that one loop. Full model-tiering infrastructure, `networks/*.yaml` manifests, persona packs, and Oxpecker are explicitly out of scope — this story proves the collapsed wiring works live on the exact question that surfaced the divergence, nothing more.

## Acceptance Criteria

1. `harness/router.py`'s `nl_discovery` branch (currently `return await nl_to_sparql.dispatch(message, session_id=session_id)`, line 36-37) is removed. Every classified intent (`unknown`, `query`, `nl_discovery`, `write`) that isn't a pending-action confirmation now reaches `agent.run()` — `nl_discovery` messages get `fuzzy=False` (confidently classified, same treatment as `query`, excluded from fuzzy-question analytics per the existing `fuzzy` kwarg semantics from Story 6.10).
2. `nl_to_sparql.py`'s LLM→SPARQL→execute capability is exposed as a new tool in `agent_tools.py`'s `READ_TOOLS` catalog (name: `query_sparql`), callable by the agent loop when `query_map`'s fixed-shape `find`/`nearby`/`network` functions (`harness/query_commands.py`) don't match the question's intent. `nl_to_sparql.dispatch()` (the `Message`-shaped, router-facing entry point) is removed; its internals (`_load_ontology_cache`, `_strip_fence`, `_escape_sparql_literal`, `FORBIDDEN` check, `_emit_gap_triple` callers) are refactored into a plain callable (e.g. `nl_to_sparql.generate_and_run(question: str, model: str, session_id: str) -> dict`) that `agent_tools.query_sparql()` wraps, matching the shape of `agent_tools.query_map()`'s passthrough to `query_commands`.
3. The hardcoded `model="anthropic/claude-sonnet-4-5"` at `nl_to_sparql.py:166` is removed. `generate_and_run()` takes `model` as a required parameter — it does not choose its own model. `harness/llm_client.py` gains a `SONNET_MODEL` constant (replacing the removed hardcoded string as the single named reference to the escalation model) alongside the existing `MODEL`/`DEFAULT_MODEL`.
4. `agent.py`'s tool-calling loop (`run()`, `harness/agent.py:29-96`) gains Tier-2 escalation, triggered by exactly two conditions (no other heuristic, no "implied intent" judgment call):
   - **Trigger A (tool error):** a `query_sparql` tool call in the current iteration returns `{"error": ...}` from `_dispatch_tool`.
   - **Trigger B (empty-but-specific):** a `query_sparql` tool call returns zero bindings AND `message.text` (case-insensitive) contains at least one of a fixed keyword list: `"how many"`, `"how much"`, `"which one"`, `"which "`, `"count"`. This list lives as a module-level constant (`agent.py::_SPECIFIC_ANSWER_KEYWORDS`, same shape as `query_commands._STATE_KEYWORDS`) — not a free-text/LLM judgment of "did the question imply specificity."

   On either trigger, the loop retries that single iteration's `complete_with_tools` call with `model=llm_client.SONNET_MODEL` instead of `llm_client.MODEL`, once per run (a per-run boolean flag, not a per-iteration counter — escalation happens at most once across all `MAX_TOOL_ITERATIONS`). A second Trigger A/B condition after the escalated retry does NOT re-escalate — it falls through to the normal `unknown_ack()`/gap-log path at loop end. This is the only place a model choice other than `llm_client.MODEL` is made anywhere in the harness.
5. Tier 0 FAQ cache stub: new `harness/faq_cache.py` holds a small, hand-seeded list of `{trigger, sparql_template, description}` entries — **exactly one seeded pair for this story** (a validated SPARQL template answering "how many spaces are open now in Berlin and which one?" using `schema:addressLocality` + `mom:openNow`, matching the pattern the map's materializer already uses correctly). `router.py` checks the FAQ cache (simple substring/keyword match against `trigger`, no embeddings/semantic-similarity infra — that's explicitly deferred) before intent classification runs. **A cache hit does NOT bypass the model call.** Concrete mechanism: `agent.run()` gains a `faq_hint: str = ""` parameter; when non-empty, `run()` builds the `system` argument to `complete_with_tools` as `bernard_agent_prompt.SYSTEM_PROMPT + "\n\n" + FAQ_HINT_TEMPLATE.format(hint=faq_hint)` instead of the bare `SYSTEM_PROMPT` — a plain string suffix, no new message role, no change to `llm_client.complete_with_tools`'s signature. `FAQ_HINT_TEMPLATE` lives in `bernard_agent_prompt.py` alongside `SYSTEM_PROMPT`, worded so Gemma treats it as a validated pattern to prefer, not an instruction to skip tool-calling. Gemma still runs, still decides how to use the hint (typically: call `query_sparql`/`query_map` with the hinted shape rather than free-generating from scratch), and the loop still produces the response through the normal tool-calling path. A cache miss passes `faq_hint=""`; behavior is unchanged from Task 2's routing. This proves the RAG-injection wiring, not the cache's coverage — one entry is sufficient DoD for this story.
6. Single gap-log mechanism: `agent_tools.log_gap()`'s `gap_kind == "ontology"` branch (`harness/agent_tools.py:217-224`, currently calling `nl_to_sparql._emit_gap_triple()`) is repointed to `_write_capability_gap()` (the existing `capability_gaps.db` SQLite table), with a new `gap_kind` column value (`"ontology"` vs `"capability"`) added to the `capability_gaps` table schema so the two kinds remain distinguishable downstream. `nl_to_sparql._emit_gap_triple()` and the `GAP_INSERT` SPARQL template (`nl_to_sparql.py:87-97, 131-144`) are deleted. Before deletion: confirmed via `grep -rn "urn:mak:gaps\|mom:OntologyGap" harness/ scripts/ infra/ web/` that no code outside `nl_to_sparql.py` and its own tests reads that graph — no downstream reader to migrate. (Grep already run 2026-07-02 during the pre-story engineering review — zero hits outside the writer and `test_nl_to_sparql.py`; re-run as part of Task 1 to catch any drift before deleting.)
7. `harness/tests/test_router.py` is updated: the `nl_discovery` regression-guard assertion (added in 6.10, asserting `nl_to_sparql.dispatch` is called unchanged) is replaced with an assertion that `nl_discovery`-classified messages call `agent.run(fuzzy=False)`, same as `query`. A new test asserts a FAQ-cache-hit message calls `agent.run(..., faq_hint=<matched entry>)` — the model call still happens, only the hint payload differs from a cache miss (which calls `agent.run()` with no hint).
8. `harness/tests/test_agent.py` gains, matching AC #4's two named triggers exactly:
   - Trigger A: mocked `query_sparql` tool call returns `{"error": ...}` → next LLM call in the same run uses `model=llm_client.SONNET_MODEL`.
   - Trigger B, positive: mocked `query_sparql` returns zero bindings, `message.text` contains `"how many"` → escalates.
   - Trigger B, negative (regression guard): mocked `query_sparql` returns zero bindings, `message.text` contains none of `_SPECIFIC_ANSWER_KEYWORDS` (e.g. a generic "tell me about spaces in Ghent") → does NOT escalate, normal empty-result handling proceeds on `llm_client.MODEL`.
   - Escalation ceiling: escalated call also fails (Trigger A or B again) → falls through to `unknown_ack()`/gap-log, no second escalation, no third model attempt.
   - `faq_hint` passthrough: `agent.run(..., faq_hint="...")` results in `complete_with_tools`'s `system` kwarg containing the hint text appended to `SYSTEM_PROMPT`; `agent.run()` with no `faq_hint` (default `""`) results in the bare `SYSTEM_PROMPT`, unchanged from pre-6.11 behavior.
9. `harness/tests/test_nl_to_sparql.py` is updated for the new `generate_and_run(question, model, session_id)` signature (model no longer defaulted/hardcoded inside the module — tests must pass it explicitly); gap-emission tests are moved/renamed to assert against `agent_tools._write_capability_gap` with `gap_kind="ontology"` instead of `_emit_gap_triple`/`urn:mak:gaps`.
10. **Live DoD gate (per [[epic_3_retro_findings]] — pytest passing is not sufficient):** two live checks against the real VPS/Oxigraph, not fixtures, before this story is marked done:
    - Live Matrix round-trip: send "how many spaces are open now in Berlin and which one?" to the deployed bot. Confirm the FAQ-cache hit is logged (hint passed into `agent.run()`, distinguishable in logs from a hint-less run) and the answer matches what the map already correctly shows for Berlin (per the confirmed-correct `addressLocality` check) — or, if `mom:openNow` is genuinely absent for those spaces, confirm the reply explains the liveness-data gap in-character rather than a bare "nothing matches."
    - A second live question that deliberately misses the FAQ cache and exercises the `query_sparql` tool through the full agent loop (e.g. a city with no seeded FAQ entry), confirmed against the live Oxigraph store directly (`curl`/SPARQL, not pytest fixture) to match the tool's returned answer.
    - Both runs' log lines and query text captured in this story's Completion Notes, same evidentiary bar as Story 6.10's live-verification rounds.

## Tasks / Subtasks

- [x] Task 1: Confirm no downstream readers of `<urn:mak:gaps>` before touching it (AC #6)
  - [x] Re-run `grep -rn "urn:mak:gaps\|mom:OntologyGap" harness/ scripts/ infra/ web/` — must return only `nl_to_sparql.py` and its own tests
  - [x] If any other hit appears, stop and flag — do not proceed with deletion until that reader is migrated or confirmed dead
- [x] Task 2: Router collapse (AC #1)
  - [x] Remove `nl_discovery` branch's `nl_to_sparql.dispatch()` call in `harness/router.py`
  - [x] Route `nl_discovery` through `agent.run(..., fuzzy=False)`, same call shape as the existing `query` branch
  - [x] Remove now-unused `import nl_to_sparql` from `router.py` if `dispatch()` was its only use
- [x] Task 3: Fold `nl_to_sparql` into the tool catalog (AC #2, #3)
  - [x] Refactor `nl_to_sparql.dispatch()` into `nl_to_sparql.generate_and_run(question, model, session_id)` — same internals (ontology cache, `FORBIDDEN` check, `_strip_fence`, `_escape_sparql_literal`), returns a dict (bindings + sparql text + count), not a pre-formatted Bernard string
  - [x] Remove hardcoded model from `nl_to_sparql.py:166`; caller supplies `model`
  - [x] Add `agent_tools.query_sparql(question: str, model: str) -> dict` wrapping `nl_to_sparql.generate_and_run()`
  - [x] Add `query_sparql` tool schema to `READ_TOOLS` in `agent_tools.py`, wire into `agent.py::_dispatch_tool`
  - [x] Add `llm_client.SONNET_MODEL` constant
- [x] Task 4: Tier-2 escalation inside `agent.py`'s loop (AC #4, #8)
  - [x] Add `agent.py::_SPECIFIC_ANSWER_KEYWORDS = ("how many", "how much", "which one", "which ", "count")` module constant
  - [x] Track per-run escalation state (has this run already retried once on `SONNET_MODEL`?) — a local boolean, not a counter
  - [x] Trigger A: `query_sparql` tool result is `{"error": ...}` → escalate
  - [x] Trigger B: `query_sparql` tool result has zero bindings AND `message.text.lower()` contains any of `_SPECIFIC_ANSWER_KEYWORDS` → escalate
  - [x] On trigger (A or B, first occurrence only), retry that iteration's `complete_with_tools` call with `model=llm_client.SONNET_MODEL`
  - [x] Second Trigger A/B after the escalated retry → existing `unknown_ack()`/gap-log fallback, no further retry
- [x] Task 5: Tier-0 FAQ cache stub (AC #5, #7)
  - [x] New `harness/faq_cache.py`: seeded list with one entry (Berlin-open-spaces validated SPARQL template)
  - [x] `router.py` checks FAQ cache before `intent_classifier.classify()`; hit → `agent.run(..., faq_hint=entry.description + entry.sparql_template)`; miss → `agent.run()` with default `faq_hint=""`
  - [x] Add `FAQ_HINT_TEMPLATE` string constant to `bernard_agent_prompt.py` (worded as "here's a validated pattern for a similar question, prefer it if it fits" — not an instruction to skip tools)
  - [x] `agent.py::run()` gains `faq_hint: str = ""` param; when non-empty, `system` arg to `complete_with_tools` becomes `bernard_agent_prompt.SYSTEM_PROMPT + "\n\n" + bernard_agent_prompt.FAQ_HINT_TEMPLATE.format(hint=faq_hint)`; when empty, `system` is the bare `SYSTEM_PROMPT` (byte-identical to pre-6.11) — no other change to loop control flow, tool dispatch, or iteration count
- [x] Task 6: Single gap-log mechanism (AC #6)
  - [x] Add `gap_kind` column to `capability_gaps` table (migration-safe `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE ... ADD COLUMN` guard for existing DB files)
  - [x] Repoint `log_gap(gap_kind="ontology")` to `_write_capability_gap(..., gap_kind="ontology")`
  - [x] Delete `nl_to_sparql._emit_gap_triple`, `GAP_INSERT`, and the now-unused `sparql_client.run_update` call site in that module (confirm no other caller of `run_update` breaks)
- [x] Task 7: Tests (AC #7, #8, #9)
  - [x] `test_router.py`: nl_discovery→agent.run(fuzzy=False) assertion; FAQ-cache-hit-bypasses-classify assertion
  - [x] `test_agent.py`: Tier-2 escalation-on-failure and escalation-does-not-loop-twice cases
  - [x] `test_nl_to_sparql.py`: updated for `generate_and_run(question, model, session_id)` signature; gap assertions moved to `capability_gaps`/`gap_kind="ontology"`
- [x] Task 8: Live VPS verification (AC #10) — rounds 1-3 (PREFIX-injection fix) deployed and reasoned through 2026-07-02; rounds 4-7 (2026-07-03) surfaced and fixed real bugs unrelated to 6.11's SPARQL-generation ACs (Matrix mention detection, thread duplication, travel command parsing, gap-logging persistence/reliability) during the same live-debugging session — see Completion Notes. AC #10 closed in round 7: a live "which spaces near Ghent/Brussels have laser cutters" question, cross-checked directly against live Oxigraph via `ssh hetzner` SPARQL queries, confirmed Bernard's confirmed/unconfirmed distinction is correct (OpenFab Brussels has `mom:endpointUrl`, Timelab Ghent doesn't) — not hallucinated.
- [x] Task 9: Scope check — confirm no `networks/*.yaml` manifest work, no persona-pack work, no Oxpecker work was added; confirm `query_commands.find`/`nearby`/`network` and their `!mom find` literal-command caller are untouched (only the NL paths collapse, not the literal-command path)

## Dev Notes

- **Orchestrator decision (roundtable, 2026-07-02):** `agent.py`'s tool-calling loop stays the outer harness, unchanged in shape. `nl_to_sparql.py`'s direct-SPARQL-generation capability is not a competing top-level path — it becomes one tool among others (`read_space`, `query_map`, now `query_sparql`, `log_gap`, `propose_write`) that the loop calls. This resolves the Winston/Nicolas fork: the handoff's three-tier model (`mom_handoff_2026-07-02.md`) described direct-SPARQL-with-cache-and-tiering, which read as closer to `nl_to_sparql.py`'s shape than `agent.py`'s tool-calling shape — but Nicolas confirmed live that the *orchestrator* (routing, escalation, gap logging, Matrix I/O) is `agent.py`, and Tier 0/2 are additions in front of and inside that loop, not a replacement architecture.
- **Why this collapses two disagreeing systems, not three parallel ones:** per the live grep in `router.py:25-40`, `query_commands.dispatch()` is already a dead unwired stub (found during 6.10) with no callers besides the still-present-but-orphaned `query_commands.find`/`nearby`/`network` functions used by `agent_tools.query_map` and the literal `!mom find` command — that path is fine and untouched. The only real fork was `nl_discovery` → `nl_to_sparql.dispatch()` (RDF gap graph, hardcoded Sonnet) vs. everything else → `agent.run()` (SQLite gap tables, Gemma default). This story closes that fork.
- **FAQ cache is intentionally dumb, and it is RAG, not a bypass.** One seeded entry, substring match, no embeddings, no ranking. Nicolas explicitly corrected the initial design here: a cache hit does not skip the model call — it injects the matched template/description as context so Gemma's tool-calling loop still runs and still decides, just with a validated pattern to lean on instead of generating from scratch. The point of this slice is proving that injection wiring (router checks cache before classify, hit or miss both still reach `agent.run()`) exists and works live, not building a good cache or a cache-as-shortcut. Expanding coverage is explicitly deferred to a follow-up story once there's real fuzzy-question volume data (from Story 6.10's `fuzzy_questions` table) to know which questions are worth pre-validating templates for.
- **Tier-2 escalation trigger is narrow by design**, matching the roundtable's "escalate on validation-gate failure or detected complexity," not "escalate whenever unsure." Broadening the trigger conditions is a tuning question for after live data exists, not this story.
- If implementing this story seems to require building out the full `networks/*.yaml` manifest system, persona packs, or Oxpecker integration described elsewhere in the handoff — stop, that's out of scope, flagged explicitly by the operator for a later story.

### Project Structure Notes

- Modified files: `harness/router.py`, `harness/agent.py`, `harness/agent_tools.py`, `harness/nl_to_sparql.py`, `harness/llm_client.py`
- New files: `harness/faq_cache.py`, `harness/tests/test_faq_cache.py` (if FAQ-cache logic warrants its own unit tests beyond the router integration test)
- Untouched: `harness/query_commands.py`, `harness/commands.py` (literal `!mom find` path), `harness/bernard_agent_prompt.py` (no prompt changes required by this story unless Tier-2 escalation needs a prompt-visible signal — check during Task 4; if so, treat as additive per the 6.10 Rule 7/8 precedent, don't rewrite existing rules)

### References

- `_bmad-output/planning-artifacts/mom_handoff_2026-07-02.md` — target three-tier architecture this story's Tier 0/2 slices are drawn from
- [Source: harness/router.py#L13-43] — current `route()` dispatch, all four branches this story touches or leaves alone
- [Source: harness/agent.py#L29-96] — the loop Tier-2 escalation is added inside
- [Source: harness/agent_tools.py#L213-224] — `log_gap`'s ontology/capability split, collapsing to one table
- [Source: harness/nl_to_sparql.py#L87-97,131-144,166] — `GAP_INSERT`/`_emit_gap_triple` (deleted) and the hardcoded model (removed)
- [[project_query_dispatch_was_dead_stub]] — why `query` already routes through `agent.run()`, precedent this story extends to `nl_discovery`
- [[feedback_llm_must_not_claim_untaken_actions]] — Rule 8 precedent from 6.10; keep in mind if Tier-2 escalation ever needs prompt-visible framing
- [[epic_3_retro_findings]] — pytest passing ≠ done; live integration is the real DoD, directly why AC #10 / Task 8 is not deferrable for this story
- [[feedback_integration_testing]] — mock tests hide protocol bugs; live integration required

## Scope Guardrails

- **Collapse-first, tier-later**: this story proves ONE routing engine with a minimal Tier 0 stub and a narrow Tier 2 trigger. It does not build out full semantic-cache infrastructure, does not build a general model-tiering policy engine, and does not expand FAQ coverage beyond the one seeded pair.
- **Out of scope, explicitly deferred per the handoff**: full `networks/*.yaml` manifest system, persona packs, Oxpecker integration. Do not touch these while "in here."
- **`query_commands.py`'s `find`/`nearby`/`network` and the literal `!mom find` command are untouched.** Only the two NL-question paths (`nl_discovery` direct-dispatch vs. `agent.run()`) collapse into one. The map's own materializer/GeoJSON query path (confirmed correct via `addressLocality` on 2026-07-02) is not part of this story at all.
- **No new gap-storage mechanism.** The collapse reuses the existing `capability_gaps` SQLite table (adding one `gap_kind` column), it does not invent a third store.
- This story does not close out the handoff's full target architecture — only the fork Nicolas and Winston raised tonight. Remaining handoff scope (cache expansion, tiering policy generalization, curated-FAQ authoring workflow) gets its own follow-up story once this slice has run live.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via Claude Code dev-story workflow.

### Debug Log References

None — no runtime debugging needed; implementation matched the roundtable-converged design in Dev Notes on first pass. Full local test run: `cd harness && python -m pytest tests/ -q --ignore=tests/test_matrix_adapter.py` → 110 passed, 3 pre-existing unrelated failures in `test_commands.py` (confirmed pre-existing via `git stash` baseline — a test-isolation issue in `bernard.read_only_ack()` voice-loading, untouched by this story). `test_matrix_adapter.py` is excluded from local runs — pre-existing, unrelated `ModuleNotFoundError: No module named 'nio'` (matrix-nio not installed in local venv).

### Completion Notes List

- Tasks 1–7 and 9 implemented and unit-tested (44 tests across `test_router.py`, `test_agent.py`, `test_nl_to_sparql.py`, new `test_faq_cache.py`; all passing).
- **Task 8, round 1 (2026-07-02, operator-deployed, log evidence from `ssh hetzner` / `docker logs maps-agent-bot`):** Question "where can I go in Brussels to visit makerspaces? which space are open right now?" — traced live:
  1. `intent.classified intent=nl_discovery` → routed to `agent.run()` (confirms the 6.11 collapse: no more direct `nl_to_sparql.dispatch()`).
  2. Gemma (`google/gemma-4-26b-a4b-it-20260403`) tried `query_map`/`query_sparql` across 3 iterations; the `query_sparql` call generated malformed SPARQL → Oxigraph `400 Bad Request` → `sparql.nl_select_failed`.
  3. **Trigger A fired correctly**: next `llm.completed` used `model=anthropic/claude-4.5-sonnet-20250929` — Tier-2 escalation wiring confirmed working end-to-end live.
  4. **Bug found:** Sonnet, once escalated, did not call `query_map`/`query_sparql` to search — it guessed 6 specific Brussels-makerspace slugs (`hsbxl`, `ko-lab`, `superlab`, `urlab`, `voidwarranties`, one more) and called `read_space` on each; all failed `"No space found for slug ..."`.
  5. `agent.max_tool_iterations_exceeded` (loop capped at 4) — no budget left to recover after the escalated iteration's failed guesses → fell through to bare `unknown_ack()`.
  - **Root cause, two parts:** (a) `MAX_TOOL_ITERATIONS = 4` left no room for a final answer after an escalation retry consumed an iteration; (b) `bernard_agent_prompt.py` rule 3 said "never fabricate values" but didn't explicitly forbid inventing a slug to probe `read_space` with — Sonnet treated a guessed slug as different from a guessed field value.
  - **Fixes applied (staged, NOT yet deployed as of this writing):** `harness/agent.py::MAX_TOOL_ITERATIONS` 4→6; `bernard_agent_prompt.py` rule 3 extended to explicitly require `query_map`/`query_sparql` for discovery questions before any `read_space` call, forbidding slug-guessing. 110 local tests still pass (`test_tier2_escalation_ceiling_no_second_retry` updated to derive its iteration count from `agent.MAX_TOOL_ITERATIONS` instead of a hardcoded 4).
  - **Course correction (operator feedback, same session):** the model-swap fix above treats the symptom, not the cause. The actual weak point is `nl_to_sparql.NL_TO_SPARQL_SYSTEM` — it handed the LLM raw OWL class/property definitions (from the ontology CONSTRUCT query) and a *wrong* fixed example graph name (`urn:mak:space/<slug>` as if literal), with zero worked SPARQL examples. Every real query in `query_commands.py` actually uses a `GRAPH ?g { ... FILTER(STRSTARTS(STR(?g), "urn:mak:space/")) }` wildcard (each space has its own named graph) and typed-literal booleans (`mom:openNow "true"^^xsd:boolean`) — none of that was in the prompt the SPARQL-generating model sees. Fixed properly: rewrote `NL_TO_SPARQL_SYSTEM` to include the correct graph-wildcard pattern, the `mom:endpointUrl`-presence "confirmed vs. seeded" rule, and two worked example queries lifted directly from `query_commands.py`'s proven, already-correct SPARQL — model-agnostic grounding, not a model-tier band-aid. Also fixed `faq_cache.py`'s Berlin template to match the same proven triple-pattern style (was using an untyped `FILTER(?openNow = true)` comparison instead of the typed-literal triple pattern used everywhere else). Also added `sparql=sparql` to the `sparql.generated`/`sparql.nl_select_failed` log lines — the original 400 in round 1 couldn't be diagnosed because the failing SPARQL text was never logged, only the exception message.
  - **Task 8, round 2 (2026-07-02, operator-deployed with the round-1 fixes live, same question re-asked):** router/escalation wiring confirmed correct again — `intent=nl_discovery` → `agent.run()`; Gemma called `query_sparql`; `sparql.generated` log now shows the query text (the round-1 logging fix worked) and it exactly matched `NL_TO_SPARQL_SYSTEM`'s worked example: correct `GRAPH ?g` wildcard, `STRSTARTS` filter, `mom:openNow "true"^^xsd:boolean` typed literal. **Still got `sparql.nl_select_failed` with `400 Bad Request`, identical to round 1.** The prompt-grounding fix was necessary but not sufficient.
    - **Real root cause, found by inspecting the logged query text directly:** the generated SPARQL uses `mom:`/`schema:`/`xsd:` prefixed names but contains **zero `PREFIX` declarations** — `NL_TO_SPARQL_SYSTEM` tells the model the prefixes exist in its own system-prompt text but never instructs the model to *repeat* them in its output, and `nl_to_sparql.py` never injected them itself (unlike `query_commands.py`, which hardcodes a `PREFIX` string and prepends it to every query it builds). Confirmed directly against the live Oxigraph endpoint: `curl` the exact query without `PREFIX` lines → `400`; same query with `PREFIX mom:`/`PREFIX schema:` prepended → `200`.
    - **Fix:** added `nl_to_sparql.PREFIX_BLOCK` + `_ensure_prefixes()` — strips any `PREFIX`/`BASE` lines the model emitted (in case it did include some, possibly wrong/incomplete ones) and unconditionally prepends the canonical four-prefix block. Wired into `generate_and_run()` right after `_strip_fence()`. This makes the fix **model-agnostic by construction** — correctness no longer depends on any model reliably repeating boilerplate in its output, matching the operator's explicit binding constraint for this whole investigation. 3 new tests added (`test_ensure_prefixes_adds_block_when_missing`, `test_ensure_prefixes_strips_model_emitted_prefix_lines`, `test_generate_and_run_output_always_has_prefixes`) — 113 local tests passing (same 3 pre-existing unrelated failures).
    - **Task 8 round 3 (re-deploy + re-test with the PREFIX fix) is still outstanding** — this is the fix expected to actually close the loop; operator to deploy and confirm the Brussels question returns a real open/closed answer. Story stays at `review` until confirmed.
  - **Separately, a real data-model gap surfaced (not a bug in this story's code, logged for future scoping, not fixed here):** "makerspace" was treated as a `schema:knowsAbout` specialty tag and correctly returned 0 matches — because space *type* (hackerspace/fablab/makerspace) has no populated axis anywhere in the pipeline. `iop:Makerspace` is declared in `iop.ttl` (`rdfs:subClassOf iop:Place`) but is never asserted (`a iop:Makerspace`) by any ingestion code — confirmed via repo-wide grep, zero hits outside the ontology file itself. Operator decision: scope this as its own follow-up story (ingestion-pipeline + ontology work, not an NL-prompt tweak) rather than fixing inline here — deferred, see project memory.
- **Task 8, rounds 4-6 (2026-07-03, live-debugging continued on the same VPS/Matrix session, three unrelated real bugs found and fixed — none are 6.11 SPARQL-generation ACs, all surfaced because live-testing 6.11 IS live-testing the whole `!mom`/`@bernard` surface):**
  - **Round 4 — `!mom travel` multi-word origin + hardcoded fallback radius.** `commands.py`'s `travel` verb used `text.split(maxsplit=2)`, so a multi-word origin like "openfab ozu" was truncated to "openfab" and the remainder ("ozu 20min by bike") failed to parse as an hours+mode argument. Fixed with `_TRAVEL_ARGS_RE`, a regex that peels the trailing `{duration}[min|h] [by mode]` off the end of the remainder instead of a positional split — works for any-length origin names. Separately, the "confirmed empty, real search succeeded" reply branch (`bernard.nearby_empty_ack`) displayed a radius computed with a hardcoded `hours_val * 80` (car-speed constant) regardless of travel mode — fixed to use the same `_FALLBACK_SPEED_KMH` mode-aware map already introduced for the ORS-timeout fallback branch. 3 new tests (`test_travel_multi_word_origin_is_not_truncated`, plus the two pre-existing fallback-speed tests still passing).
  - **Round 5 — thread-duplication bug traced to its real cause: a second live bot process, not Matrix/Element.** The original live report ("`!mom` reply in a thread ALSO posts a full duplicate to the main room feed") was chased through two dead-end fixes: (a) added MSC3440 `is_falling_back`/`m.in_reply_to` fallback fields to `matrix_adapter.py::send()` — live-verified to NOT fix it; (b) removed those same fields entirely, bare `m.thread` relation only — also did not fix it, and was reverted. **Root cause, found by comparing `MATRIX_USER_ID` between the local dev compose stack and the VPS container:** both run as the identical Matrix account (`@bernard:mapsofmaking.org`), so when the local dev stack is up at the same time as live-testing the VPS, **two independent bot processes** both receive and both reply to every event in the shared room — the VPS bot posts correctly into the thread, the local bot (seeing the same event with no thread-relation context of its own construction) posts into the main feed. Confirmed via VPS logs showing exactly one `message.responded` per command (ruling out a code-level double-send) plus direct confirmation that stopping the local container stopped the duplicate. `matrix_adapter.py::send()` reverted to the original `is_falling_back`+`m.in_reply_to` version (spec-compliant, was never the bug). No code fix applies here — this is an operational/environment hazard, captured in `project_shared_matrix_account_ghost_bot` memory as a 6.7 (Bernard onboarding/deployment) design input: needs either separate Matrix accounts per environment or a same-account single-session guard.
  - **Round 6 — `@bernard` mention detection was silently dropping messages for some Matrix clients.** Live report: `@jason_p` asking "`Bernard: what tools do you have?`" got zero reply (not even an error ack) while an apparently-identical message from Nicolas worked. Root cause found by pulling the raw event JSON directly from Dendrite's Postgres (`roomserver_event_json`): jason's client renders a real mention pill via `formatted_body`'s `<a href="matrix.to/#/@bernard:...">` link but sends **no `m.mentions` field at all**, and the plaintext `body` is just the display name `"Bernard: ..."` with no `@` — so neither of `matrix_adapter.py`'s two existing mention signals (`m.mentions`, literal `@bernard` in body) fired. Two intermediate false leads were disproved with live evidence before finding this: (1) a federation-delay/pre-boot-filter theory, killed because jason's `!mom close`/`!mom welcome` literal commands worked fine in the same window; (2) treating the rendered `"Bernard: ..."` text as proof the raw body lacked `@` — operator correctly pushed back that Element's pill-to-plaintext copy rendering doesn't reflect the underlying event content, which sent the investigation back to pulling the actual event JSON instead of reasoning from a screenshot. Consulted the Matrix spec directly (Context7 `/matrix-org/matrix-spec`) to confirm the right layering: `m.mentions` is the sole canonical signal (MSC3952/Matrix 1.7+, and spec v1.17 removed the legacy display-name-body-match push rules entirely), so bare-name matching (e.g. a permissive `"Bernard:"`/`"Bernard,"` prefix, tried and explicitly reverted mid-session on operator's "Bernard s'en fiche" false-positive objection) is the wrong direction — the formatted_body pill-link is presentation HTML, not a spec mention signal, but checking for the bot's own mxid inside it is the correct pragmatic fallback for non-MSC3952-compliant clients. Fixed: `matrix_adapter.py::_on_message` now also checks the bot's mxid substring inside `content.formatted_body`. Documented as `project_matrix_mention_client_fragmentation` memory (6.7 input).
  - **Round 6 follow-up — trigger suppression inside quotes/code (feature request, not a bug):** operator wanted to be able to show another user the exact `@bernard`/`!mom` syntax to type (e.g. inside `` `inline code` ``, a ```` ```fenced block``` ````, or a `> ` quote) without Bernard itself firing on the demonstration. `!mom` was already immune (`main_matrix.py` gates on `text.startswith("!mom")`, and a quoted/code demonstration never starts the message that way). The mention path needed an explicit fix: `matrix_adapter.py` now strips fenced/inline-code spans and blockquote lines (plaintext regex) and their HTML equivalents (`<pre>`/`<code>`/`<blockquote>`, formatted_body) before running the literal-`@bernard`/pill-substring checks — `m.mentions` is left unstripped/authoritative since a client only emits it for a genuine intentional mention, never for code-span text. 4 new tests covering inline code, blockquote, HTML `<code>`-wrapped pill, and the negative case (a real mention elsewhere in the same message as an unrelated code span still fires).
  - Full local suite after rounds 4-6: 134 passed, same 3 pre-existing unrelated failures (`test_commands.py` low-power-level `read_only_ack()` voice-string mismatch, confirmed pre-existing via `git stash` baseline earlier in this story's history — untouched by any of today's changes).
- **Task 8, round 7 (2026-07-03, AC #10 closed) — live SPARQL cross-check + three follow-on findings, all logged for future stories rather than fixed here:**
  - **AC #10 evidence.** Asked Bernard "which spaces near Ghent have laser cutters?" (→ "No spaces near Ghent with laser cutters are currently in the dataset") and "which spaces near Brussels have laser cutters?" (→ "OpenFab in Brussels has a laser cutter"). Queried live Oxigraph directly via `ssh hetzner` (bypassing the bot entirely) for all `schema:knowsAbout` "laser" matches in both cities: Ghent has Timelab (laser, no `mom:endpointUrl`); Brussels has FabLab ULB, fablab iMAL (no `endpointUrl`) and OpenFab (`mom:endpointUrl` present). Bernard's confirmed/unconfirmed distinction exactly matches ground truth — not a hallucination, not a lucky guess. AC #10 closed.
  - **Follow-on fix, shipped this round:** `nl_to_sparql.py`'s `mom:endpointUrl` triple was previously required (hard-filters out unconfirmed spaces silently); changed to `OPTIONAL` + `BIND(BOUND(?e) AS ?confirmed)` so a single query surfaces both confirmed and unconfirmed matches. `bernard_agent_prompt.py` gained rule 3a: report confirmed matches with confidence, but if zero confirmed exist alongside unconfirmed ones, say so — the same graceful-but-truthful nudge `!mom find`/`nearby` already do via `seeded_note_ack`, previously missing from the NL/agent path entirely. Live-verified after deploy: "No confirmed spaces found near Ghent with laser cutters. One unconfirmed space, Timelab, matches your request."
  - **Follow-on fix, shipped this round:** `log_gap` was write-only — no tool or command could ever read back logged gaps, and separately `/app/tasks` (the SQLite DB's location) had no volume mount in `docker-compose.yml`, so every `make publish` redeploy silently wiped all logged data. Added `agent_tools.read_recent_gaps(limit)` and a coordinator-gated `!mom gaps [N]` command (`commands.py`, `bernard.gaps_report`/`gaps_empty_ack`), and added the missing `../data/bot-tasks:/app/tasks` bind mount. 3 new tests (`test_gaps_from_low_power_level_is_refused`, `test_gaps_reports_recent_rows`, `test_gaps_empty_reports_none_logged`).
  - **Two new findings investigated and deliberately NOT fixed — logged to `deferred-work.md` instead:** (1) city-name search (both the web Find UI and `nl_to_sparql.py`'s SPARQL) does a literal substring match against `addressLocality`, missing spaces whose locality is an annexed-district name distinct from the parent city (live example: Ingegno Maker Space, `addressLocality "Drongen"`, a real deelgemeente of Ghent since a 1977 merger, real high-fidelity coords ~5km from Ghent center, laser-tagged — invisible to both "ghent" searches). Root cause is architectural (no metro-area/geo-radius fallback for city filters), not a data or ingestion bug — confirmed via direct Oxigraph query on Ingegno's graph. (2) `log_gap` itself is pure LLM self-report with no code-level backstop — live-tested two genuinely gap-shaped questions (a weather-factoring capability request, an ontology-less "partnership" query) and neither called `log_gap` nor landed in the separate `fuzzy_questions` analytics table (which only captures `intent="unknown"`-classified messages). Both are scoped as new stories in `deferred-work.md`, not folded into 6.11.
  - Full local suite after round 7: 137 passed (3 new gaps tests), same 3 pre-existing unrelated failures.
- Design decisions made resolving ACs to code, where the story text left a degree of freedom:
  - `agent_tools.query_sparql(question, model, session_id="")` is always invoked by `agent.py::_dispatch_tool` with `model=llm_client.MODEL` — never `SONNET_MODEL` — so that AC #4's claim ("the only place a model choice other than `llm_client.MODEL` is made anywhere in the harness") stays literally true. Tier-2 escalation upgrades the *next* top-level `complete_with_tools` call, not the `query_sparql` tool's internal SPARQL-generation call.
  - Tier-2 escalation is implemented as: on Trigger A/B (evaluated right after a `query_sparql` tool result lands), set a `escalate_next_call` flag consumed by the very next loop iteration's `complete_with_tools` call — matching AC #8's "next LLM call in the same run uses `model=llm_client.SONNET_MODEL`" wording exactly. A separate `escalated` boolean (never reset) enforces the once-per-run ceiling.
  - The old `bernard.nl_result_ack`/`nl_empty_ack`/`nl_gap_ack`/`nl_invalid_sparql_ack` functions in `bernard.py` are left in place, now unused by `nl_to_sparql.py` (which returns a plain dict per AC #2) — `bernard.py` isn't in this story's modified-files list and removing them wasn't requested; flagging as dead code for a future cleanup pass rather than deleting unrequested.
  - `sparql_client.run_update()` itself is untouched (only its `nl_to_sparql.py` call site is deleted) — it's a generic client method, not story-specific.
- Regression check: `query_commands.py`, `commands.py` (literal `!mom find`/`update`/`open`/`close` path), and `bernard_agent_prompt.py`'s existing 8 rules are byte-unchanged (only `FAQ_HINT_TEMPLATE` appended). No `networks/*.yaml`, persona-pack, or Oxpecker code was touched.

### File List

- `harness/router.py` — modified (nl_discovery collapsed into agent.run(); FAQ-cache check before classify(); nl_to_sparql import removed)
- `harness/agent.py` — modified (faq_hint param + system-prompt injection; query_sparql tool dispatch; Tier-2 escalation state machine + `_SPECIFIC_ANSWER_KEYWORDS`; `_dispatch_tool` gains `session_id` param)
- `harness/agent_tools.py` — modified (`query_sparql` tool function + schema; `capability_gaps` table gains `gap_kind` column + migration guard; `log_gap` collapsed to single `_write_capability_gap` call)
- `harness/nl_to_sparql.py` — modified (`dispatch()` replaced by `generate_and_run(question, model, session_id="")` returning a dict; `_emit_gap_triple`/`GAP_INSERT`/`_escape_sparql_literal` deleted; hardcoded model removed)
- `harness/llm_client.py` — modified (`SONNET_MODEL` constant added)
- `harness/bernard_agent_prompt.py` — modified (`FAQ_HINT_TEMPLATE` constant added, additive only)
- `harness/faq_cache.py` — new (Tier-0 FAQ cache stub, one seeded Berlin-open-spaces entry)
- `harness/tests/test_router.py` — modified (nl_discovery test updated for agent.run(); FAQ-cache hit/miss tests added)
- `harness/tests/test_agent.py` — modified (ontology-gap test repointed to capability_gaps table; migration-guard test; query_sparql dispatch test; 4 Tier-2 escalation tests; 2 faq_hint passthrough tests)
- `harness/tests/test_nl_to_sparql.py` — rewritten for `generate_and_run(question, model, session_id="")` signature; dispatch-shaped tests replaced with dict-return assertions
- `harness/tests/test_faq_cache.py` — new (3 tests: seeded-trigger hit, case-insensitivity, miss)
- `harness/commands.py` — modified (round 4: `_TRAVEL_ARGS_RE` regex-based travel arg parsing replaces positional split, supports multi-word origins; `nearby_empty_ack` radius now uses mode-aware `_FALLBACK_SPEED_KMH` instead of hardcoded `*80`)
- `harness/isochrone.py` — modified (`OriginAmbiguousError` + ranked-then-ambiguity-checked `_resolve_origin`, from an earlier live round this same session)
- `harness/bernard.py`, `harness/bernard_voice.yaml` — modified (`travel_ambiguous_origin_ack` / `travel_ambiguous_origin` voice string)
- `harness/matrix_adapter.py` — modified (round 6: mention detection also checks bot mxid inside `formatted_body`; fenced/inline-code/blockquote stripping — plaintext and HTML — before the literal-body/pill mention checks run; `is_falling_back`/`m.in_reply_to` thread-relation fields reverted to original spec-compliant form after round 5 ruled out Matrix/Element as the thread-duplication cause)
- `harness/main_matrix.py` — modified (round 5/6: `message.dropped_not_addressed` debug log on the previously-silent not-addressed-to-bot early return, so a real mention-detection miss is traceable instead of invisible)
- `harness/tests/test_commands.py`, `harness/tests/test_query_commands.py` — modified (travel multi-word-origin, mode-aware fallback speed, origin-ambiguity tests)
- `harness/tests/test_matrix_adapter.py` — modified (formatted_body-pill mention test, trigger-suppression tests for inline code/blockquote/HTML code span, thread-relation fallback-fields test restored)
- `harness/nl_to_sparql.py` — modified (round 7: `mom:endpointUrl` made OPTIONAL with `BIND(BOUND(?e) AS ?confirmed)`, worked examples updated)
- `harness/bernard_agent_prompt.py` — modified (round 7: new rule 3a, report confirmed matches with confidence + honest nudge on unconfirmed-only/mixed results)
- `harness/agent_tools.py` — modified (round 7: `read_recent_gaps(limit)` read-side helper for `!mom gaps`)
- `harness/commands.py` — modified (round 7: `!mom gaps [N]` command, coordinator-gated, lazy-imports `agent_tools` to avoid a circular import)
- `harness/bernard.py`, `harness/bernard_voice.yaml` — modified (round 7: `gaps_report`/`gaps_empty_ack`)
- `harness/tests/test_commands.py` — modified (round 7: 3 new `!mom gaps` tests)
- `infra/docker-compose.yml` — modified (round 7: added missing `../data/bot-tasks:/app/tasks` bind mount — `capability_gaps.db`/`fuzzy_questions.db` were being silently wiped on every redeploy)
- `_bmad-output/implementation-artifacts/deferred-work.md` — modified (round 7: two new deferred-work entries — city-search locality-granularity gap, `log_gap` self-report reliability gap)

### Change Log

- 2026-07-02: Story 6.11 implemented — collapsed the two disagreeing NL-question paths (`router.py`'s direct `nl_to_sparql.dispatch()` vs. `agent.run()`) into a single orchestrator. `nl_to_sparql`'s SPARQL generation is now the `query_sparql` tool inside `agent.py`'s tool-calling loop. Added Tier-0 FAQ-cache hint injection (`faq_cache.py`, one seeded entry) and narrow Tier-2 Sonnet escalation (two fixed triggers, once per run). Retired the RDF `<urn:mak:gaps>` writer — both ontology and capability gaps now write to the existing `capability_gaps` SQLite table, distinguished by a new `gap_kind` column. 44 new/updated unit tests, all passing; 3 pre-existing unrelated test failures confirmed via baseline diff.
- 2026-07-02: Live VPS verification (round 1, operator-deployed): confirmed the 6.11 collapse and Tier-2 escalation both work end-to-end live, but found a real bug — Sonnet guesses space slugs instead of searching once escalated, and the 4-iteration cap left no room to recover. Fixed: `MAX_TOOL_ITERATIONS` 4→6, prompt rule 3 extended to forbid slug-guessing. Round-2 re-test still outstanding — story stays at `review`.
- 2026-07-02/03: Live VPS verification rounds 2-3: grounded `NL_TO_SPARQL_SYSTEM` with worked query examples, then found and fixed the real root cause of the persistent `400 Bad Request` — generated SPARQL had zero `PREFIX` declarations; fixed model-agnostically via code-level `_ensure_prefixes()` injection rather than relying on any model reliably repeating boilerplate.
- 2026-07-03: Continued live-debugging session surfaced and fixed three bugs unrelated to 6.11's SPARQL-generation ACs, discovered because live-testing 6.11 exercises the whole `!mom`/`@bernard` bot surface: (1) `!mom travel` multi-word origins truncated by positional string splitting, plus a hardcoded car-speed constant leaking into the mode-aware fallback-radius display; (2) a "thread reply duplicates to main feed" report chased through two dead-end Matrix-relation-field fixes before the real cause was found — a second bot process on the same shared `@bernard` Matrix account (local dev stack + VPS both live simultaneously), not a Matrix/Element bug at all; (3) `@bernard` mentions silently dropped for at least one real Matrix client that renders a mention pill via `formatted_body` only, with no `m.mentions` field and no literal `@` in the plaintext body — fixed by also checking the bot's mxid inside `formatted_body`, plus added trigger-suppression so a mention/command shown inside code or a quote (to demonstrate syntax to another user) doesn't fire. Two new project memories captured for the future Epic 6.7 (Bernard onboarding/deployment) story: shared-account ghost-bot hazard, and Matrix client mention-signal fragmentation. AC #10's exact evidentiary bar (FAQ-cache-hit log line + a second live query cross-checked directly against Oxigraph) not re-captured this session — Task 8 stays open.
- 2026-07-03 (round 7): AC #10 closed — a live Ghent/Brussels laser-cutter question, cross-checked directly against Oxigraph via `ssh hetzner`, confirmed Bernard's confirmed/unconfirmed distinction is correct, not hallucinated. That check surfaced `nl_to_sparql.py` silently hard-filtering out unconfirmed matches (fixed: OPTIONAL + BOUND flag) and the agent prompt never nudging about unconfirmed results the way `!mom find`/`nearby` already do (fixed: new rule 3a). Also shipped `!mom gaps` (`log_gap` was write-only with no read path) and fixed a real data-loss bug found in the process — `/app/tasks` had no volume mount, so every redeploy silently wiped logged gaps. Two further findings investigated live and deliberately deferred rather than fixed: a city-search locality-granularity gap (annexed-district localities like Ghent's "Drongen" invisible to substring city search — architectural, not a data bug) and a `log_gap` self-report reliability gap (two real live gap-shaped questions went completely unlogged, model didn't call the tool). Both logged to `deferred-work.md` as new stories. Full local suite: 137 passed, same 3 pre-existing unrelated failures. Story marked `done`.
